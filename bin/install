#!/bin/sh -x

##############################################################################
#
# Install MySelf.
#
##############################################################################

load_config() {

    . $(dirname $0)/../conf/defaults.conf
}

main() {
    load_config

    local dir="${1:-$gh_me}"

    git clone ${gh_uri} $dir
}

main "$@"
