#!/bin/sh -x

##############################################################################
#
# Install MySelf.
#
##############################################################################

install_do() {
    local dir="${1:-$gh_me}"

    git clone ${gh_uri} $dir
}

main() {
    load_config

    install_do "$@"
}

main "$@"
