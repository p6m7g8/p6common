# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
name: Build
on:
  pull_request: {}
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Deps
        run: /bin/sh scripts/deps.sh
      - name: Test
        run: make test
    container:
      image: scottw/alpine-perl
